# Downloadable PDFs (v1 placeholders)

These are minimal valid PDF files generated for development. Replace each file with the real Maths Mania content PDF when ready.

Files are served at `/downloads/*.pdf` and linked from `/resources`.
